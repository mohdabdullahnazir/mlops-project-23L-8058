# Data Directory

Place the local training dataset at `data/dataset.csv`.

Raw dataset files are excluded from Git to keep the repository lightweight and to prevent accidental publication of data.

Student ID: 23L-8058