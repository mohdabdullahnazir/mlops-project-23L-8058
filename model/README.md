# Model Directory

The training script saves the generated model artifact in this directory.

Serialized model files are excluded from Git because they are generated artifacts and can be reproduced by running the training pipeline.

Student ID: 23L-8058
